{
  "name": "opencode-webhook",
  "version": "0.0.2",
  "description": "A lightweight, asynchronous webhook plugin for OpenCode that sends events to a configured webhook endpoint.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "opencode",
    "plugin",
    "webhook"
  ],
  "author": "dtinth",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/dtinth/opencode-webhook"
  },
  "dependencies": {},
  "files": [
    "index.js"
  ]
}
